csim.c is a cache simulator, which can parse command arguments to initilize a cache. 
traces files contain some example operations on cache. The simulator can parse a file line by line and execute cache access.
